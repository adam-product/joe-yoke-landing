import * as unplugin from 'unplugin';
import { StreamingPluginOptions } from 'unhead/stream/unplugin';

type UnheadReactStreamingOptions = Pick<StreamingPluginOptions, 'mode'>;
/**
 * Bundler-agnostic streaming SSR plugin for `@unhead/react`. Exposes
 * `.vite()`, `.webpack()`, `.rspack()`, `.rollup()`, `.esbuild()` builders
 * via unplugin. Consumers should prefer the unified per-bundler entries
 * at `@unhead/react/{vite,webpack,rspack,rollup}`.
 */
declare const unheadReactStreamingPlugin: unplugin.UnpluginInstance<UnheadReactStreamingOptions | undefined, boolean>;

export { unheadReactStreamingPlugin as u };
export type { UnheadReactStreamingOptions as U };
