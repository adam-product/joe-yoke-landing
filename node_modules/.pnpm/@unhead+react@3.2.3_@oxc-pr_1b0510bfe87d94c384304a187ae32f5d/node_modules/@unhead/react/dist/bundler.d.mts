import * as _unhead_bundler_framework from '@unhead/bundler/framework';
import { UnheadFrameworkOptions } from '@unhead/bundler/framework';
import { U as UnheadReactStreamingOptions } from './shared/react.DhK8GAQ6.mjs';
import 'unplugin';
import 'unhead/stream/unplugin';

type UnheadReactOptions = UnheadFrameworkOptions<UnheadReactStreamingOptions>;
/**
 * Unified bundler plugin factory for `@unhead/react`. Returns an object with
 * per-bundler dispatch methods (`vite`, `webpack`, `rspack`, `rollup`).
 *
 * @example
 * ```ts
 * import { Unhead } from '@unhead/react/bundler'
 * export default defineConfig({ plugins: [react(), ...Unhead({ streaming: true }).vite()] })
 * ```
 */
declare const Unhead: (options?: UnheadFrameworkOptions<UnheadReactStreamingOptions>) => _unhead_bundler_framework.UnheadBundlerFactory;

export { Unhead };
export type { UnheadReactOptions };
