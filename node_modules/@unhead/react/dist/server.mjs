import { createElement } from 'react';
import { U as UnheadContext } from './shared/react.BvsYt2da.mjs';
export { createHead, prepareTemplate, renderSSRHead, transformHtmlTemplate } from 'unhead/server';

function UnheadProvider({ children, value }) {
  return createElement(UnheadContext.Provider, { value }, children);
}

export { UnheadProvider };
